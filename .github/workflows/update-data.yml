name: Atualizar dados BCB/IBGE

on:
  schedule:
    # 12:00 UTC = ~09:00 no horario de Brasilia (UTC-3)
    # ajuste o horario se quiser rodar em outro momento do dia
    - cron: '0 12 * * *'
  workflow_dispatch: {}   # permite rodar manualmente pela aba Actions

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install requests pandas statsmodels

      - name: Buscar dados do BCB
        run: python scripts/fetch_bcb_data.py

      - name: Buscar dados de Atividade (PIB/FBCF/Hiato)
        run: python scripts/fetch_pib_data.py

      - name: Commitar se houver mudancas
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/fiscal_data.json data/activity_data.json
          if git diff --staged --quiet; then
            echo "Sem mudancas nos dados."
          else
            git commit -m "Atualiza dados BCB/IBGE - $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
          fi
